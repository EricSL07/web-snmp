// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Modelo para os usuários
model User {
  id        String   @id @default(cuid())
  login     String   @unique
  password  String // A senha será armazenada como um hash
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// Futuramente, você pode adicionar modelos para os switches e seus dados
// model Switch {
//   id        String   @id @default(cuid())
//   name      String
//   ipAddress String   @unique
//   ...
// }

model Switch {
  id        String   @id @default(cuid())
  hostname  String   @unique // Nome do Host
  ipAddress String   @unique // IP
  group     String   // Grupo do Host
  location  String?  // Localização (opcional)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}